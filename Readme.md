简要说明：公众号后台是没法直接和用户直接信息交互的，所有消息都是通过腾讯服务器中转。所以会有以下几个条件
1，在公众号后台绑定我们的服务器接口，用来接收腾讯服务器给我们转发的消息的。
   注意1：端口必须为 80
       2：后台绑定接口为get，接收消息用post。接口地址必须一样，只是请求方式不同。
2，我们的服务器配置需要有公众号的appID和appsecret，用来标识我们的身份
3，Token ，保证消息的合法性

